In Absyn.fs, I added:
  | If of expr * expr * expr

In ExprLex.fsl, I added:
  | '?'             { THEN }
  | ':'             { ELSE }

In ExprPar.fsy, I added:
1. 
  %token THEN ELSE
2.  
  | Expr THEN Expr ELSE Expr       { If($1, $3, $5) }

See project files for more.
